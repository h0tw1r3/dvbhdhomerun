General:

* Use geralds patch for DKMS building.


Kernel Module:

* Need to support DVB-T also.

* Remove unneeded DVB functions.

* Init:
  * Need error checking in _init function

* Debug
  * Decide which functions to put under each bit pattern.

* SNR ratio in femon etc. Can that be derived from the HDHomeRun.


UserHDHomerun:

* Option to run as DVB-T. Probably configuration file
(/etc/dvbhdhomerun).

* Add locking of tuner.

* Run as a daemon via init.d.



Bugs:

