General:

* Check documentation for references.

* upstart, check sidste post fra silicondust.


Kernel Module:

* Remove unneeded DVB functions.

* Init:
  * Need error checking in _init function

* Debug
  * Decide which functions to put under each bit pattern.


UserHDHomerun:

* MAX_TUNERS=2 get rid of that, use values from struct.

* Use strstr on "atsc"/"cablecard" or "dvbt" to autodetect.

* Add locking of tuner?

* Timestamp in log.

